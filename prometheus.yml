global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - prometheus_alerts.yml

scrape_configs:
  - job_name: 'hosting_guard'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:8000']
