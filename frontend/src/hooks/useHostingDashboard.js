/**
 * @deprecated Use useDashboardData instead.
 * Kept for backward compatibility — re-exports the canonical hook under the old name.
 */
export { useDashboardData as useHostingDashboard } from './useDashboardData';
